#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Masukkan jumlah bintang paling atas: ";
    cin >> n;  // Input

    int i = n;
    // Perulangan do-while
    do {
        int j = 0;
        while (j < i) {
            cout << "*";
            j++;
        }
        cout << endl;  // Pindah ke baris baru setelah mencetak satu baris bintang
        i--;
    } while (i > 0);

    return 0;
}
